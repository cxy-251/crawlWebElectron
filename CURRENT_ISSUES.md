# CURRENT_ISSUES.md

## 当前实际运行现象

1. Electron 应用能打开，左侧浏览器区和右侧工具区都可见。
2. 左侧能打开快手上传页并识别真实页面 capabilities。
3. 继续草稿后能进入快手发布编辑页，并识别作品描述、合集、同城页展示、发布时间等能力。
4. 本地 HTTP API 已监听 `127.0.0.1:3218`。

## 本轮要修的核心问题

1. 上传新视频不是“已有编辑页写参数”，而是必须先找到上传入口并传视频，等待快手进入编辑页后才能写入页面能力。
2. 批量新视频如果遇到未发布草稿，不能自动继续旧草稿，也不能放弃草稿；应返回 `DRAFT_CONFLICT` 并暂停。
3. API 使用脚本、个人路径、合集名、发布时间配置和任务 JSON 都不能进入仓库。
4. 上库前需要清理 `.DS_Store`、构建产物、运行数据和隐私内容。

## 实际修复方案

1. `upload-single` 增加 `uploadIntent` 和 `draftPolicy`。
2. 默认新视频路径为 `new_video + pause`。
3. 新视频路径先进入上传文件入口，再设置视频文件，之后等待编辑页并写参。
4. `confirmPublish = true` 时，API 在任务进入发布前确认后点击最终发布。
5. `local-api-usage/` 作为 ignored 目录保存本机 API 使用脚本和真实常量。
6. 仓库文档只保留占位说明，不写个人路径或真实本地配置。

## 当前仍需注意

1. 草稿冲突需要人工在左侧页面处理后重试。
2. 封面弹窗、章节弹窗仍按人工辅助处理，不做绕过。
3. 批量脚本是本机私有工具，不随仓库提交。
4. 不执行 `git push`。
