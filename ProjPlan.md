<!--
Project: CrawlWebElectron
Document: 当前任务计划书（Current Task Plan）
Version: 0.1
Date: 2026-06-03
Purpose: 本文件只描述“本次要让 Codex 完成的一次工程任务”。一次 ProjPlan.md 更新，就是一次新的任务输入。
Usage:
  • Codex 执行前必须先阅读本文件
  • Codex 本次只执行本文档中的“当前任务”，不要自动推进其他任务
  • 完成后需要更新任务状态、完成说明、当前问题和下一步建议
  • 本文件需要纳入 Git 追踪，但 Codex 不允许执行 git commit、git push 或任何提交发布操作
  • Codex 只能修改文件并汇报变更，最终提交由用户人工审查后完成
-->

# CrawlWebElectron 当前任务计划书

## 0. 本次任务一句话

实现 Electron + React 的第一阶段最小可运行原型：左侧是可登录、可保持会话的网页浏览区，右侧是 React 自动化控制面板，可以提取当前网页 URL、标题和链接，并具备基础日志与本地资源安全边界。

## 1. 当前任务状态

**Status**: DONE

**当前任务**：第一阶段最小可运行原型

**执行者**：Codex

**重要限制**：本次只做这个任务，不要继续规划或实现下一阶段任务。

## 2. 项目定位

这个应用不是普通浏览器，而是一个“左侧网页登录态浏览器 + 右侧自动化工具控制台”的网页采集工作台。

左侧负责真实网页访问、登录、浏览和保持会话；右侧负责把我们的网页信息提取、网页自动化、媒体资源获取、批量下载入口等工具逐步封装成 React 操作界面。

## 3. 本次任务目标

从当前项目出发，实现一个可以运行、可以验证、方便继续扩展的 Electron + React 桌面应用基础骨架。

本次任务完成后，项目应该具备：

1. Electron 桌面应用可以启动。
2. 主界面分为左右两栏。
3. 左侧可以作为真实网页浏览区打开外部网页。
4. 左侧支持输入 URL、前进、后退、刷新和正常网页登录。
5. 网页登录态尽量持久化，关闭应用后重新打开尽量不用重新登录。
6. 右侧是 React 控制面板。
7. 右侧按钮可以获取当前网页 URL、页面标题、页面链接，并显示结果。
8. 代码中有基础可调试日志，方便定位主进程、preload、渲染进程、IPC、网页加载和脚本执行问题。
9. 项目有合理的本地资源与 GitHub 安全边界，避免 cookies、token、账号信息、真实配置、缓存、日志、下载文件等进入仓库。

## 4. 本次任务必须交付

Codex 必须把任务落到工程代码里，不要只改文档。

交付内容包括：

1. 可启动的 Electron + React 应用原型。
2. 左右分栏界面。
3. 左侧网页浏览能力。
4. 右侧 React 工具面板。
5. preload / IPC / 主进程之间的安全通信链路。
6. 获取当前 URL、标题、链接的最小功能按钮。
7. 基础日志能力。
8. 合理的 `.gitignore` 安全规则。
9. 必要的工程说明文档，例如启动方式、验证方式、主要模块职责。
10. 本文件的任务状态更新。
11. Codex 只允许修改工作区文件，不允许执行 git commit、git push、发布、部署等操作。

## 5. 技术与架构原则

Codex 不要机械创建固定目录，也不要照抄本文件里的文字当成目录结构。Codex 必须先阅读当前项目结构，再自行决定如何拆分模块。

实现时必须遵循：

1. 单一职责原则：窗口创建、网页容器、IPC、preload、安全边界、日志、页面信息提取、React UI 尽量拆清楚，不要写成一个大文件。
2. 开闭原则：后续新增网站工具、页面解析工具、下载入口时，应该尽量通过新增模块或注册工具扩展，而不是频繁修改核心窗口和核心通信代码。
3. 依赖倒置：React 渲染层不要直接依赖 Node.js、Electron 主进程、文件系统、cookies 文件路径等底层细节。
4. 接口隔离：preload 暴露给 React 的 API 要小而清晰，例如获取当前 URL、获取页面标题、提取页面链接，不要暴露一个巨大且混乱的全局对象。
5. 安全桥接：涉及网页脚本执行、本地资源访问、会话管理的能力，应通过 Electron 主进程、preload 和 IPC 进行控制。
6. 可调试：关键路径必须有日志，但日志不能泄露敏感信息。

## 6. 左侧网页区域要求

左侧网页区域应优先考虑 Electron 的 BrowserView / WebContentsView 等适合承载真实网页的方案，而不是普通 iframe。

要求：

1. 可以打开外部网页。
2. 可以输入 URL 并跳转。
3. 可以前进、后退、刷新。
4. 用户可以在网页中正常登录。
5. 尽量使用持久化 session 保存登录态，例如 Electron 持久化 partition 或其他合适方案。
6. 如果当前项目或 Electron 版本导致某种方案不可用，Codex 需要选择可运行方案，并在完成说明中解释原因。

## 7. 右侧 React 控制面板要求

右侧控制面板第一阶段只需要基础工具，不要做复杂爬虫系统。

至少实现这些按钮或入口：

1. 获取当前页面 URL。
2. 获取当前页面标题。
3. 提取当前页面链接。
4. 测试执行页面脚本或测试 IPC 通信。

执行结果需要显示在 React 面板中，方便人工验证。

## 8. 本地资源与 GitHub 安全边界

本次任务必须建立安全边界，但不要机械创建一堆空目录。

原则：

1. cookies、token、账号密码、登录态导出文件、下载缓存、临时文件、真实运行配置、日志文件、用户数据不能提交到 GitHub。
2. Git 仓库只保存源码、文档、计划书、示例配置和不含秘密的默认配置。
3. `.gitignore` 规则由 Codex 根据当前项目实际结构设计。
4. React public 目录只能放公开静态资源，不允许放私密运行资源。
5. React 渲染层不直接读取敏感文件。
6. 如果需要本地资源路径，应由 Electron 主进程统一管理，并通过安全 API 暴露必要能力。

Codex 完成后必须说明：哪些内容允许进 Git，哪些内容必须留在本地，为什么这样划分。

## 9. 日志与调试要求

本次任务需要基础日志，不需要完整监控系统。

要求：

1. 至少支持 info、warn、error 级别或等价能力。
2. 主进程启动、窗口创建、网页加载、IPC 调用、脚本执行失败等关键路径应有日志。
3. 日志可以先输出到控制台；如果输出到文件，日志文件必须被 `.gitignore` 忽略。
4. 日志不能记录 cookies、token、账号密码、完整请求头等敏感信息。
5. 完成后说明新增了哪些日志点，以及它们能帮助定位什么问题。

## 10. 本次不要做

本次任务不要做以下事情：

1. 不要做复杂爬虫逻辑。
2. 不要做多站点适配。
3. 不要做真实下载器。
4. 不要做账号池。
5. 不要做代理池。
6. 不要做数据库。
7. 不要做任务队列。
8. 不要做复杂插件系统。
9. 不要为了满足文档机械创建无意义目录或空文件。
10. 不要主动规划或执行下一阶段任务。
11. 不要执行 `git commit`、`git push`、发布、部署或任何会写入远端仓库/发布环境的操作。
12. 不要替用户决定提交历史；Codex 完成后只汇报变更，最终提交由用户人工审查后执行。

## 11. 验收标准

任务完成后，应满足：

1. 项目可以正常启动。
2. 能看到 Electron 桌面窗口。
3. 能看到左右分栏界面。
4. 左侧能打开一个外部网站。
5. 左侧能完成基本网页登录交互。
6. 关闭应用后重新打开，登录态尽量保留。
7. 右侧按钮能获取当前 URL、页面标题、页面链接。
8. 右侧能显示执行结果。
9. 代码具备基础日志，且不泄露敏感信息。
10. `.gitignore` 能保护本地私密资源、真实配置、缓存、日志、下载文件不进入 GitHub。
11. Codex 说明了主要模块职责、启动方式、验证方式、当前问题。
12. Codex 更新了本文件的任务状态、完成说明、当前问题和下一步建议。
13. Codex 未执行任何 commit、push、发布或部署操作。

## 12. 当前问题

- 已完成项目结构检查：执行前项目只有 `ProjPlan.md` 和 `.gitignore`，本次新增了最小 Electron + React + TypeScript 工程骨架。
- 当前安装到的 Electron 版本为 39.8.10。本次采用 `BrowserView` 承载左侧真实网页；它在本机烟测可运行。Electron 39 已提供 `WebContentsView` 作为更新方向，但第一阶段为保证最小原型稳定落地，暂未切换到该 API。
- 已补充 `README.md`，包含启动方式、验证方式、模块职责和安全边界。
- 登录态持久化使用 Electron 持久化 partition：`persist:crawl-web-electron`。本次已完成应用启动和默认网页加载烟测；具体第三方网站登录态是否保持，需要用户用真实账号在本机手动验证。
- 初次 `npm install` 时 Electron 默认下载源较慢，本次实际使用 `ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/ npm install` 完成依赖安装。后续如遇同类下载卡住，可继续使用该环境变量。

## 13. 完成说明

已完成第一阶段最小可运行原型。

1. 修改/新增文件：
   - `.gitignore`
   - `package.json`
   - `package-lock.json`
   - `index.html`
   - `vite.config.ts`
   - `tsconfig.electron.json`
   - `tsconfig.renderer.json`
   - `README.md`
   - `src/main/*`
   - `src/preload/*`
   - `src/renderer/*`
   - `src/shared/*`
   - `ProjPlan.md`
2. 主要模块职责：
   - `src/main/main.ts`：Electron 生命周期入口。
   - `src/main/appWindow.ts`：创建承载 React 的主窗口。
   - `src/main/webViewController.ts`：创建和控制左侧 `BrowserView`，负责导航、页面状态、持久化 session partition 和链接提取。
   - `src/main/ipc.ts`：注册主进程 IPC。
   - `src/main/navigation.ts`：限制并规范化导航 URL，仅允许 `http/https`。
   - `src/main/logger.ts`：提供 info/warn/error 控制台日志和敏感字段脱敏。
   - `src/preload/index.ts`：通过 `contextBridge` 暴露小而清晰的浏览器/调试 API。
   - `src/renderer/App.tsx`：左右分栏 UI、地址栏、导航按钮、右侧工具面板、结果区和渲染进程日志。
   - `src/shared/*`：共享 IPC channel 和类型。
3. 启动方式：
   - 首次安装：`npm install`
   - 开发启动：`npm run dev`
   - 构建后启动：`npm run start`
4. 左侧网页区验证：
   - 在地址栏输入 `https://example.com` 或其他 `http/https` 页面并打开。
   - 验证后退、前进、刷新。
   - 在真实网站登录后关闭并重启应用，检查登录态是否尽量保留。
5. 右侧控制面板验证：
   - 点击 `URL` 获取当前页面 URL。
   - 点击 `Title` 获取页面标题。
   - 点击 `Links` 提取页面链接。
   - 点击 `Ping` 测试 preload/IPC/主进程链路。
6. 日志位置：
   - 主进程日志输出在启动命令所在终端。
   - 右侧 `Logs` 区显示渲染进程操作日志。
   - 已覆盖应用启动、窗口创建、BrowserView 创建、网页加载、IPC 调用、脚本执行失败等关键路径。
7. `.gitignore` 保护内容：
   - `node_modules/`
   - `dist/`、`build/`、`out/`、`release/`
   - `.env*` 真实配置
   - `.local/`、`runtime/`、`user-data/`、`downloads/`、`cache/`、`tmp/`、`temp/`
   - `*.log`、`logs/`
   - 编辑器和系统噪音文件
8. 已验证：
   - `npm run typecheck` 通过。
   - `npm run build` 通过。
   - `npm run start` 烟测通过：窗口创建、BrowserView 创建、`https://example.com/` 加载成功、IPC handler 注册成功。
9. 当前问题：
   - 第三方网站登录态需要用户用真实账号手动验收。
   - Electron 39 推荐的新方向是 `WebContentsView`，后续如需要更长期 API 稳定性，可以在下一次任务中评估替换。
10. 下一步建议：
   - 建议用户人工运行 `npm run dev`，完成网页登录态和右侧工具按钮验收。
   - 验收通过后再由用户决定是否提交本次原型。

## 14. 给 Codex 的执行提示

请阅读本文件，并只执行本文档描述的当前任务：第一阶段最小可运行原型。

不要连续执行后续任务，不要重写任务路线系统，不要把本文档改成抽象规范集合。你可以补充必要工程文档，但本次核心交付必须是可运行的 Electron + React 原型。

禁止执行 `git commit`、`git push`、发布、部署或任何写入远端仓库/发布环境的操作。完成后只需要汇报修改了哪些文件、如何验证、当前问题和建议提交信息；最终是否提交由用户人工审查后决定。
